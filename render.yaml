services:
  - type: web
    name: ai-learning-hub-backend
    env: node
    root: .
    buildCommand: npm install
    startCommand: node server.js
    envVars:
      - key: NODE_VERSION
        value: 18.17.0